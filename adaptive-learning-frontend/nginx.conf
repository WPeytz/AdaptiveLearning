server {
    listen 8080;  # Cloud Run requires port 8080

    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri /index.html;
    }

    error_page 404 /index.html;  # Handle Vue/React history mode
}